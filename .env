# Expo Go on the Android emulator must use ADB reverse instead of the host LAN IP.
# Expo CLI loads this value even when started directly with `npx expo start`.
REACT_NATIVE_PACKAGER_HOSTNAME=127.0.0.1
